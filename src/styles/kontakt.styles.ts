import { createStyles } from '@mantine/core';

export const useStyles = createStyles((theme, _params, getRef) => ({
  wrapper: {
    padding: '20px 12px calc(100vh - 755px) 12px',
    maxWidth: '1000px',
    marginLeft: 'auto',
    marginRight: 'auto',
  },
}));
